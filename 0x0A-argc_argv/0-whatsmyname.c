#include <stdio.h>
/**
 * main - prints its name
 * @argc: number of arguments
 * @argv: array of arguments
 *
 *
 * Return: s[i]
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void) argc;
	return (0);
}
