#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

/* local variable definition */
int a;
/* while loop execution */
for (a = 0; a < 10; a++)
{
printf("%d", a);
}

printf("\n");

return (0);
}
